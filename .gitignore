# Nunca versionar segredos
.config/coolify/
coolify/env
*.token
*.pem
*_rsa
.env

# Histórico de shell: nunca em repo público (comandos, hosts, caminhos)
.zsh_history
zsh/.zsh_history
.bash_history

# Segredos
.config/secrets/
secrets/env
