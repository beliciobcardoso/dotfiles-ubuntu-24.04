# Nunca versionar segredos
.config/coolify/
coolify/env
*.token
*.pem
*_rsa
.env

# Histórico de shell: nunca em repo público (comandos, hosts, caminhos)
.zsh_history
zsh/.zsh_history
.bash_history

# Segredos
.config/secrets/
secrets/env

# Backups gerados pelo setup.sh
*.backup

# Config local de ferramentas (por máquina, não faz parte do dotfile)
.serena/
.claude/settings.local.json
